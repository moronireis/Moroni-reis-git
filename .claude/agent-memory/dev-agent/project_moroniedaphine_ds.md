---
name: Moroni & Daphine — Wedding Design System Preview
description: Single-page static design system preview for the M&D wedding site. Deployed to Vercel.
type: project
---

## Path
`/Users/moronireis/Projetos vscode/moroniedaphine/`

## Files
- `index.html` — full page (79KB), all CSS inline via `<style>`, no external CSS files
- `vercel.json` — `outputDirectory: "."`, `framework: null`
- `.vercel/` — auto-generated by Vercel CLI (gitignored)

## Deploy
- Production alias: `https://moroniedaphine.vercel.app`
- Deploy command: `vercel --prod --yes` from the directory
- Project name on Vercel: `moroniedaphine` (moronifreis-gmailcoms-projects)

## Design System
Source: `/Users/moronireis/Projetos vscode/brain/assets/branding/casamento-moroni-daphine/DESIGN_SYSTEM.md`

Key tokens applied:
- `#4A1619` burgundy, `#FFFFFF` white, `#D4AF37` gold — the ONLY 3 colors
- Font: Fraunces variable (Google Fonts) + Inter variable
- All font-variation-settings implemented: opsz 144/SOFT 50/WONK 1 for display, opsz 14/SOFT 0/WONK 0 for body
- SVG grain texture applied to white/tint sections via CSS pseudo-element + data URI
- Countdown is LIVE (JS calculates days to 2026-06-12T15:00:00-03:00)
- IntersectionObserver scroll reveals on all `.reveal` elements
- All animations gated with `prefers-reduced-motion` check

## Sections built
1. Hero — burgundy bg, wordmark in Fraunces 300 opsz 144, gold hairline divider, CSS entry animations
2. Color Palette — core tokens, functional derivatives, prohibited colors (X overlay), bridesmaids gradient
3. Typography — full 9-level scale, opsz comparison (display vs body), ampersand treatment
4. Spacing Tokens — visual bar chart for space-1 through space-10
5. Components — buttons (primary/ghost/gold-ghost), content/feature cards, underline form inputs, mini timeline, live countdown
6. Graphic Elements — frame/border demos, divider demos (thin/gold/botanical SVG/whitespace), grain texture demo
7. Motion Language — easing values, Lenis config, hero stagger sequence table, reduced-motion note
8. Section Color Logic — all 9 sections with their bg/text/accent rules visualized

## Locked design decisions (from memory)
- 3 colors ONLY: burgundy/white/gold. No cream, ivory, blush.
- Fraunces + Inter ONLY. No script fonts.
- Zero border-radius everywhere.
- Minimum 600ms transition duration.
