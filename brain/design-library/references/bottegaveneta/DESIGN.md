---
name: "Bottega Veneta"
colors:
  primary: "#000000"
  secondary: "#272727"
  tertiary: "#009539"
  neutral: "#767676"
  surface: "#ffffff"
  text: "#000000"
  text-muted: "#767676"
  border: "#d8d8d8"
  error: "#eb0000"
  success: "#009539"
  disabled: "#bfbfbf"
  surface-warm: "#f1f3f2"
  border-light: "#ececec"
  mid-gray: "#474747"
typography:
  display-hero:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "3.5625rem"
    fontWeight: "400"
    lineHeight: "1.15"
    letterSpacing: "0em"
    features: ""
  display-large:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "2.1875rem"
    fontWeight: "400"
    lineHeight: "1.2"
    letterSpacing: "0em"
    features: ""
  section-heading:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "1.5rem"
    fontWeight: "400"
    lineHeight: "1.3"
    letterSpacing: "0em"
    features: ""
  subheading-large:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "1.25rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  subheading:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "1.0625rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  body-large:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "1rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  body:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "0.875rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  body-small:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "0.75rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  button:
    fontFamily: "bottegaveneta-bold-webfont, sans-serif"
    fontSize: "0.875rem"
    fontWeight: "700"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  button-small:
    fontFamily: "bottegaveneta-bold-webfont, sans-serif"
    fontSize: "0.75rem"
    fontWeight: "700"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  link:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "0.875rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  caption:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "0.75rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  caption-small:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "0.625rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  nav:
    fontFamily: "bottegaveneta-bold-webfont, sans-serif"
    fontSize: "0.75rem"
    fontWeight: "700"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  h1:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "3.5625rem"
    fontWeight: "400"
    lineHeight: "1.15"
    letterSpacing: "0em"
    features: ""
  h2:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "2.1875rem"
    fontWeight: "400"
    lineHeight: "1.2"
    letterSpacing: "0em"
    features: ""
  h3:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "1.5rem"
    fontWeight: "400"
    lineHeight: "1.3"
    letterSpacing: "0em"
    features: ""
  h4:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "1.25rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  body-lg:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "1rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  body-md:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "0.875rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  body-sm:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "0.75rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  label:
    fontFamily: "bottegaveneta-regular-webfont, serif"
    fontSize: "0.75rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
  mono:
    fontFamily: "monospace"
    fontSize: "0.875rem"
    fontWeight: "400"
    lineHeight: "1.4"
    letterSpacing: "0em"
    features: ""
rounded:
  none: "0px"
  sm: "3px"
  md: "5px"
  lg: "10px"
  full: "9999px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "16px"
  lg: "24px"
  xl: "40px"
preview_tokens:
  button_primary_bg: "#000000"
  button_primary_text: "#ffffff"
  button_primary_border: "#000000"
  button_secondary_bg: "transparent"
  button_secondary_text: "#000000"
  button_secondary_border: "#000000"
  button_tertiary_text: "#000000"
  surface_bg: "#ffffff"
  card_bg: "#ffffff"
  text: "#000000"
  text_muted: "#767676"
  border: "#d8d8d8"
  accent: "#000000"
  button_radius: "0px"
  card_radius: "0px"
  input_radius: "0px"
components:
  button-primary:
    bg: "#000000"
    text: "#ffffff"
    border: "#000000"
    radius: "0px"
    padding: "15px 0"
    font: "14px bottegaveneta-bold-webfont weight 700"
    hover_bg: "#000000"
  button-secondary:
    bg: "transparent"
    text: "#000000"
    border: "#000000"
    radius: "0px"
    padding: "15px 0"
    font: "14px bottegaveneta-bold-webfont weight 700"
    hover_bg: "transparent"
  button-disabled:
    bg: "transparent"
    text: "#bfbfbf"
    border: "#bfbfbf"
    radius: "0px"
    padding: "15px 0"
    font: "14px bottegaveneta-bold-webfont weight 700"
  card:
    bg: "#ffffff"
    border: "transparent"
    radius: "0px"
    shadow: "none"
    padding: "0px"
  input-text:
    bg: "#ffffff"
    text: "#000000"
    border: "#000000"
    radius: "0px"
    padding: "9px 0"
    focus_border: "#000000"
  badge-default:
    bg: "transparent"
    text: "#000000"
    border: "transparent"
    radius: "0px"
    padding: "0px"
    font: "10px bottegaveneta-regular-webfont weight 400"
  nav-header:
    bg: "#ffffff"
    text: "#000000"
    border_bottom: "#d8d8d8"
    backdrop_filter: "none"
    height: "auto"
  tab-default:
    bg: "transparent"
    text: "#000000"
    border: "transparent"
    radius: "0px"
    padding: "0 0 15px"
    font: "14px bottegaveneta-regular-webfont weight 400"
---

## 1. Visual Theme & Atmosphere

Bottega Veneta's digital presence is a masterclass in luxury restraint. The interface operates on a strict black-and-white foundation with zero chromatic accent — no brand color punctuates the UI because the house's identity IS absence of ornamentation. Where other luxury brands lean on a signature color (Tiffany blue, Cartier red, Hermes orange), Bottega Veneta's signature is the craft itself: the intrecciato weave, the leather texture, the product photography. The website exists purely to frame these objects.

Typography is delivered through a custom proprietary serif face — `bottegaveneta-regular-webfont` — that carries the weight of the entire visual identity. It is used at weight 400 universally; there is no display-weight theatrics, no tracking manipulation, no typographic showmanship. The bold variant (`bottegaveneta-bold-webfont`) appears only in navigation and button contexts where functional emphasis demands it. This typographic discipline — one face, two weights, natural spacing — mirrors the house's philosophy that the work speaks without decoration.

Surfaces are flat. There are no shadows, no elevation layers, no glassmorphism, no gradients. Depth is communicated entirely through photography (which is allowed to dominate) and the thin gray borders (`#d8d8d8`) that quietly separate content zones. The system is intentionally anti-digital: it reads like a printed catalog translated to screen, where the paper is white, the ink is black, and the craft is in the making, not the presentation.

The single most distinctive visual choice is the absolute commitment to zero color accent in the interface. Every interactive element — links, buttons, navigation — is rendered in pure black `#000000`. Even the "green" states (`#009539` for availability) are functional necessities, not brand expressions. This creates a visual environment where product photography holds total chromatic authority.

**Key Characteristics:**
- Pure black `#000000` as both brand identity and UI action color — no separate accent
- Custom proprietary serif typeface at exclusively weight 400 (regular) and 700 (bold only for buttons/nav)
- Zero border-radius on all interactive elements — sharp rectangles everywhere
- No shadows, no elevation, no blur — intentionally flat
- Photography-dominant: UI recedes completely, imagery commands attention
- Monochrome gray scale from `#272727` through `#767676` to `#d8d8d8` — never saturated
- White surface with no warm or cool tint — pure `#ffffff`
- Minimal state differentiation: hover states rely on underlines and font-weight, not color shifts
- Navigation uses 12px bold uppercase — functional signposting, not decorative type
- Error red (`#eb0000`) is the only saturated color in the system, reserved for validation

## 2. Color Palette & Roles

### Primary

- **Black** (`#000000`): Identity color. Used for headings, body text, links, buttons, navigation, focus outlines. Bottega Veneta's brand presence on digital IS black — the absence of a chromatic accent is itself the brand signal. 662 declarations across the stylesheet.

### Brand & Dark

- **Near-Black** (`#272727`): Secondary heading tone and form label color. Used where a softer-than-pure-black is needed for supporting text. 39 declarations.
- **Dark Gray** (`#353535`): Tertiary dark tone for specific UI contexts. 2 declarations.

### Accent Colors

This system intentionally has no chromatic accent. Product photography carries all color. The closest to an accent is the functional green and red:

- **Bottega Green** (`#009539`): Availability indicators, success confirmations, toggle-checked states. 24 declarations. This is NOT a brand accent — it is a functional necessity.
- **Deep Green** (`#008533`): Secondary green variant for tooltips. 2 declarations.

### Interactive

- **Hover text** remains `#000000` — no color change on hover for most elements
- **Hover background** (`#eeeeee`): Subtle warm gray for link/tab hover backgrounds
- **Tab hover** (`#f1f3f2`): Warm off-white for tab hover states
- **Active text** remains `#000000` — no state differentiation by color

### Neutral Scale

- **Black** (`#000000`): Primary text, headings, links — 662 uses
- **Near-Black** (`#272727`): Labels, secondary headings — 39 uses
- **Mid-Dark Gray** (`#474747`): Tertiary text, specific hover — 17 uses
- **Mid Gray** (`#767676`): Muted text, descriptions, card metadata — 169 uses
- **Silver Gray** (`#bfbfbf`): Disabled states, inactive text — 36 uses
- **Light Gray** (`#d8d8d8`): Borders, dividers — 47 uses

### Surface & Borders

- **White** (`#ffffff`): Page background, card background, modal background — 226 uses
- **Warm Off-White** (`#f1f3f2`): Tab hover, button variant background — 19 uses
- **Cool Off-White** (`#eeeff1`): Secondary surface variant — 2 uses
- **Light Silver** (`#ececec`): Lighter divider/border — 7 uses
- **Border** (`#d8d8d8`): Primary hairline — 47 uses
- **Dark Border** (`#d1d1d1`): Secondary border — 3 uses

### Shadow Colors

The system does not use colored shadows. The single shadow declaration found (`inset 0 1px 3px #000`) uses pure black and appears only on specific button states. This is a functional shadow, not a decorative elevation system.

### Color Philosophy

Bottega Veneta's palette is constructed around a single conviction: the product is the only thing that should carry color. The interface is a white gallery wall with black typographic labels. This is not minimalism as style — it is the house's founding philosophy ("When your own initials are enough") expressed as a digital system. The gray scale exists purely for hierarchy: `#000000` for primary information, `#272727` for secondary, `#767676` for tertiary, `#bfbfbf` for inactive. No tone carries brand meaning; every tone serves a functional rank. The functional greens and reds are clinical markers, deliberately unstyled, because styling them would imply the UI has a personality beyond framing the craft.

## 3. Typography Rules

### Font Family

The primary typeface is `bottegaveneta-regular-webfont` — a custom proprietary serif commissioned for the house. It is self-hosted in woff2/woff/ttf/eot formats with `font-display: optional` (meaning it will not flash unstyled — if the font doesn't load instantly, the page renders without it). The bold variant `bottegaveneta-bold-webfont` is a separate face file, not a variable-weight axis. A personalization variant `bottegaveneta-regular-perso-webfont` exists with `font-display: swap` for monogramming/engraving contexts. No monospace companion exists in the brand system; `monospace` is used only for code/debug contexts. No OpenType features are explicitly declared.

### Hierarchy

| Role | Font | Size | Weight | Line Height | Letter Spacing | Features | Notes |
|------|------|------|--------|-------------|----------------|----------|-------|
| display-hero | bottegaveneta-regular-webfont | 3.5625rem (57px) | 400 | 1.15 | 0em | — | Full-width editorial headlines |
| display-large | bottegaveneta-regular-webfont | 2.1875rem (35px) | 400 | 1.2 | 0em | — | Section hero, campaign titles |
| section-heading | bottegaveneta-regular-webfont | 1.5rem (24px) | 400 | 1.3 | 0em | — | Category headers, collection names |
| subheading-large | bottegaveneta-regular-webfont | 1.25rem (20px) | 400 | 1.4 | 0em | — | Product names on cards, drawer headings |
| subheading | bottegaveneta-regular-webfont | 1.0625rem (17px) | 400 | 1.4 | 0em | — | Alert banners, mid-size labels |
| body-large | bottegaveneta-regular-webfont | 1rem (16px) | 400 | 1.4 | 0em | — | Long-form descriptions |
| body | bottegaveneta-regular-webfont | 0.875rem (14px) | 400 | 1.4 | 0em | — | Default body text, product details, links |
| body-small | bottegaveneta-regular-webfont | 0.75rem (12px) | 400 | 1.4 | 0em | — | Labels, secondary nav, form helpers |
| button | bottegaveneta-bold-webfont | 0.875rem (14px) | 700 | 1.4 | 0em | — | CTAs, primary actions |
| button-small | bottegaveneta-bold-webfont | 0.75rem (12px) | 700 | 1.4 | 0em | — | Compact button variant |
| nav | bottegaveneta-bold-webfont | 0.75rem (12px) | 700 | 1.4 | 0em | — | Navigation items, category tabs |
| link | bottegaveneta-regular-webfont | 0.875rem (14px) | 400 | 1.4 | 0em | — | Inline links, footer links |
| caption | bottegaveneta-regular-webfont | 0.75rem (12px) | 400 | 1.4 | 0em | — | Metadata, legal text, form labels |
| caption-small | bottegaveneta-regular-webfont | 0.625rem (10px) | 400 | 1.4 | 0em | — | Badges, notification counts |

### Principles

- The brand typeface is a serif — never substitute with a generic sans-serif like Arial or Helvetica for headings. The serif character IS the brand voice on screen.
- Weight 400 is the default for ALL text including headlines. Weight 700 appears ONLY in navigation and buttons — never in headings, body, or descriptive text.
- Zero letter-spacing everywhere. No tracking is applied at any size — the typeface is designed to set naturally without adjustment.
- Line-height is consistently 1.4 for body contexts and tightens to 1.15-1.2 only at display sizes (35px+). There is no decorative leading.
- The bold face (`bottegaveneta-bold-webfont`) is a separate font file, not a weight axis. Do not interpolate between weights.
- Font-display is `optional` for the regular face, meaning the page will render with the system fallback if the custom font takes longer than ~100ms to load. This reflects luxury confidence: the typography is important but the experience should never feel broken.

## 4. Components

### Buttons

**Primary Black** (`button-primary`)
- Background: `#000000`
- Text: `#ffffff`
- Border: `1px solid #000000` (via outline property)
- Padding: 15px 0
- Radius: 0px — zero, sharp rectangle
- Font: 14px bottegaveneta-bold-webfont weight 700
- Hover: background remains `#000000`, no visible change — the CTA is confident and static
- Use: "Add to bag", "Checkout", primary conversion actions

**Secondary Outline** (`button-secondary`)
- Background: `transparent`
- Text: `#000000`
- Border: `1px solid #000000` (via outline property)
- Padding: 15px 0
- Radius: 0px
- Font: 14px bottegaveneta-bold-webfont weight 700
- Hover: background remains transparent, text stays black
- Use: "Continue shopping", "Find in store", secondary actions

**Disabled** (`button-disabled`)
- Background: `transparent`
- Text: `#bfbfbf`
- Border: `1px solid #bfbfbf`
- Radius: 0px
- Font: 14px bottegaveneta-bold-webfont weight 700
- Use: Out-of-stock sizes, unavailable actions

The system uses a single button vocabulary: outlined rectangles, black or transparent, with no ghost/pill/gradient variants. Emphasis is conveyed by fill (black = primary, transparent = secondary), not by shape or color variation.

### Cards & Containers

**Product Card** (`card`)
- Background: `#ffffff`
- Border: `transparent` — no visible border on product cards
- Radius: 0px — sharp edges
- Shadow: none — the system is intentionally flat
- Padding: 0px — content sits flush
- Font: 14px bottegaveneta-regular-webfont weight 400
- Text color: `#767676` for price/metadata, `#272727` for product name
- Hover: text shifts to `#000000` on interaction
- Use: Product listing tiles, collection items

Product cards are intentionally raw containers — no decoration, no elevation, no border. The product image fills the entire card width, and text metadata sits below with minimal spacing. The card IS the photograph.

### Inputs & Forms

**Text Input** (`input-text`)
- Background: `#ffffff`
- Text: `#000000`
- Placeholder: `#767676`
- Border: bottom-border only or thin outline — zero border-radius
- Radius: 0px — all 6 border-radius declarations on inputs are `0`
- Padding: 9px 0 (underline style) or 10px 15px (boxed style)
- Focus border: `1px solid #000000`
- Font: 14px bottegaveneta-regular-webfont weight 400
- Use: Newsletter email, search, checkout forms

**Label** (`label`)
- Text: `#272727`
- Font: 12px bottegaveneta-regular-webfont weight 400

**Error Text** (`error_text`)
- Text: `#eb0000`
- Font: 12px bottegaveneta-regular-webfont weight 400

### Badges / Tags / Pills

**Badge** (`badge-default`)
- Background: `transparent`
- Text: `rgba(0,0,0,0.4)` — 40% opacity black (resolved: `#000000` at reduced opacity)
- Border: `transparent`
- Radius: 0px
- Padding: 0px
- Font: 10px bottegaveneta-regular-webfont weight 400
- Use: Cart count indicators, notification markers

### Navigation

**Header Navigation** (`nav-header`)
- Background: `#ffffff` — solid white, no transparency or blur
- Text: `#000000` (desktop) / alternates with `#ffffff` on dark overlays
- Border bottom: `#d8d8d8` — thin divider separating header from content
- Backdrop filter: none — solid surface, no glassmorphism
- Height: auto — variable based on banner configuration
- Font: 12px bottegaveneta-bold-webfont weight 700
- Transition: `opacity 1s` — slow, deliberate fade for menu reveals

**Tab** (`tab-default`)
- Background: `transparent`
- Text: `#000000`
- Border: `transparent`
- Padding: 0 0 15px
- Font: 14px bottegaveneta-regular-webfont weight 400
- Hover: background shifts to `#f1f3f2` warm off-white

### Decorative Elements

The Bottega Veneta digital system has virtually no decorative elements. There are no dashed borders, gradient accents, decorative lines, or ornamental dividers. The sole decorative motif is the thin `1px solid #d8d8d8` border used as a section separator. Even the logo is rendered as a simple SVG wordmark — no emblem, no icon, no symbol. The intrecciato weave pattern appears exclusively in product photography, never as a UI texture or background pattern.

## 5. Layout Principles

### Spacing System

Base unit is 16px (1rem), with a scale built in increments of 4px. The system favors generous vertical spacing between sections (80-160px range) while keeping horizontal content padding moderate (16-24px on mobile, 60px on desktop).

Key spacing values from the CSS: 4px (0.25rem), 5px (0.3125rem), 8px (0.5rem), 10px (0.625rem), 15px (0.9375rem), 16px (1rem), 20px (1.25rem), 24px (1.5rem), 25px (1.5625rem), 27px (1.6875rem), 30px (1.875rem), 32px (2rem), 40px (2.5rem), 60px (3.75rem), 80px (5rem), 100px (6.25rem), 160px (10rem).

### Grid & Container

The layout uses a full-bleed approach where hero imagery and campaign content span the entire viewport width. Content containers max out at the browser width — there is no explicit `max-width` cap on product grids. Product listing grids use 2-column on mobile expanding to 3 or 4 columns on desktop, with zero gap between product tiles (the photographs tile edge-to-edge). Category navigation uses a slide-from-right panel that transforms `translateX(100%)` → `translateX(0)`.

### Whitespace Philosophy

Whitespace on Bottega Veneta's site is not a design choice — it is a material. The home page deploys full-viewport imagery sections separated by deliberate emptiness, creating a rhythm that mirrors the experience of walking through a physical Bottega Veneta boutique where products are spaced far apart on vast neutral surfaces. This whitespace communicates the same luxury signal as an empty gallery wall: the absence is intentional, and the absence itself costs money (the space could have been filled with more products, more text, more persuasion — the brand chooses not to). Section padding regularly exceeds 80px vertically, and product card grids leave no gap between tiles, creating a photographic mosaic effect that treats the grid as a single composed image rather than individual units.

### Border Radius Scale

- **0px** (none): Buttons, inputs, cards, navigation, tabs — the dominant radius across the entire system. This is the brand signature shape.
- **3px** (0.1875rem): Applied only to specific link contexts — a barely-perceptible softening
- **5px**: Applied to specific UI corners — rare
- **10px** (0.625rem): Applied to specific button variants and card contexts — infrequent
- **50%**: Circle — used only for round notification badges and close buttons

The system overwhelmingly defaults to 0px. Any border-radius above 0 is an exception, not a design pattern.

## 6. Depth & Elevation

| Level | Treatment | Use |
|-------|-----------|-----|
| Flat (default) | No shadow, no elevation | All components — cards, buttons, inputs, navigation |
| Border | `1px solid #d8d8d8` | Section dividers, form field borders, table lines |
| Overlay | `rgba(0,0,0,0.2)` background | Modal backdrops, drawer overlays |
| Inset | `inset 0 1px 3px #000` | Specific pressed-button states only |

The system is intentionally zero-elevation. Depth is communicated via border + surface contrast, not shadow. There are no ambient shadows, no card elevation, no floating elements. The modal overlay uses a simple `rgba(0,0,0,0.2)` backdrop — not a blur, not a gradient, just a flat semi-transparent wash.

### Shadow Philosophy

Bottega Veneta's digital system has no shadow vocabulary because shadow implies a layered interface — elements floating above a surface, creating a z-axis hierarchy of importance. The house rejects this metaphor. Every element exists on the same plane as every other element, as if printed on the same sheet of paper. This flatness is not a trend choice (the site predates the flat-design movement); it is a philosophical position: the product is real and dimensional, the interface is not. The interface should never try to simulate physicality — that is the product's domain.

## 7. Do's and Don'ts

### Do's

- **Do** use `bottegaveneta-regular-webfont` at weight 400 for all headings, body text, and descriptions — the regular weight IS the brand voice
- **Do** use `bottegaveneta-bold-webfont` at weight 700 exclusively for buttons and navigation — nowhere else
- **Do** use pure black `#000000` for all links — Bottega Veneta links are never blue, never underlined by default
- **Do** use 0px border-radius on all buttons and inputs — sharp rectangles are the brand shape vocabulary
- **Do** let product photography fill the full width of its container with no padding, rounded corners, or decorative framing
- **Do** use `#767676` for secondary text (prices, metadata, descriptions) and `#272727` for form labels
- **Do** use `1px solid #d8d8d8` for all dividers and borders — never thicker, never darker
- **Do** use `transition: all 0.2s linear` for micro-interactions — BV uses linear easing and 200ms duration as the standard

### Don'ts

- **Don't** use any chromatic accent color (blue, green, red, purple) as a brand element — BV's digital accent is black
- **Don't** apply border-radius greater than 0px to buttons, cards, or inputs — rounded corners read as SaaS, not luxury
- **Don't** use weight 300 (light) for any text — BV's lightest weight is 400. Light weights read as "tech-elegant," not craft-confident
- **Don't** add box-shadow to cards or containers — the system is intentionally flat, shadows break the printed-catalog metaphor
- **Don't** use backdrop-filter or glassmorphism — BV's surfaces are solid white, not translucent
- **Don't** apply letter-spacing to any text at any size — the proprietary typeface sets naturally without tracking adjustments
- **Don't** use gradient fills on any element — no gradient backgrounds, no gradient text, no gradient borders
- **Don't** use `#eb0000` (red) for anything other than error states and sale indicators — it is not a brand color
- **Don't** use bounce, spring, or elastic easing — BV uses linear easing; even ease-out is rare. The motion is clinical, not playful
- **Don't** add decorative icons or emoji to the UI — BV uses two icon fonts (`icons`, `masked-text-webfont`) for functional icons only (arrows, close, search, bag)

## 8. Responsive Behavior

### Breakpoints

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | < 768px (48em) | Single-column layout, hamburger nav, full-width product tiles in 2-col grid |
| Tablet | 768px - 1024px | Menu expands, product grid to 3 columns, navigation reveals horizontal |
| Desktop | > 1024px | Full horizontal navigation, product grid to 3-4 columns, campaign sections at full-bleed |

### Touch Targets

Button padding of `15px 0` with full-width behavior ensures adequate touch targets on mobile. Navigation hamburger menu and close buttons use ample hit areas. The slide-from-right panel pattern (`translateX(100%)` entry) provides a native-feeling navigation drawer on mobile.

### Collapsing Strategy

- **Typography**: Display sizes (57px hero) scale down, but the custom serif remains at all breakpoints. Font sizes generally maintain their rem values, relying on viewport-width imagery to create responsive hierarchy rather than font-size breakpoint jumps.
- **Grid**: Product grids collapse from 4 → 3 → 2 columns. Hero sections maintain full-bleed at all sizes.
- **Navigation**: Horizontal category bar on desktop collapses to a full-screen slide-from-right overlay on mobile with stacked category lists.
- **Campaign content**: Full-viewport campaign images maintain aspect ratio and bleed edge-to-edge regardless of viewport width.

### Image Behavior

Product images are the centerpiece of the responsive system. They maintain their aspect ratio and fill available width completely — no padding, no margins, no rounded corners. Campaign hero images use full-viewport-height treatments. Product grid images are uniformly sized within their column structure, creating a perfectly aligned photographic mosaic.

## 9. Agent Prompt Guide

### Quick Color Reference

- Primary / Brand: Bottega Veneta Black (`#000000`)
- Secondary text: Near-Black (`#272727`)
- Body text: Pure Black (`#000000`)
- Muted text: Mid Gray (`#767676`)
- Disabled: Silver (`#bfbfbf`)
- Surface: Pure White (`#ffffff`)
- Warm surface: Off-White (`#f1f3f2`)
- Border: Light Gray (`#d8d8d8`)
- Error / Sale: Red (`#eb0000`)
- Success / In-stock: Green (`#009539`)
- Link color: Black (`#000000`)
- Button CTA fill: Black (`#000000`)

### Example Component Prompts

> "Create a hero section with a full-bleed product photograph spanning 100vw. Overlay text in the lower-left corner: headline at 57px bottegaveneta-regular-webfont weight 400, color #ffffff, line-height 1.15, letter-spacing 0em. Below the headline, a CTA link in 12px bottegaveneta-bold-webfont weight 700, color #ffffff, uppercase, text-decoration underline. No background overlay on the image — rely on photographic darkness for contrast. Zero border-radius everywhere."

> "Create a product card with zero border-radius, zero shadow, zero border. Product image fills 100% width with no padding. Below the image: product name at 14px bottegaveneta-regular-webfont weight 400, color #272727. Price at 14px weight 400, color #767676. No hover effect on the card container — only the text link has a subtle underline on hover. Padding: 0 on the card, 8px top margin between image and text."

> "Create a badge showing cart count: 10px bottegaveneta-regular-webfont weight 400, color rgba(0,0,0,0.4), no background, no border, no border-radius. Position absolute relative to the bag icon."

> "Create a navigation header: solid white (#ffffff) background, no backdrop-filter, no shadow. Logo SVG centered. Navigation links at 12px bottegaveneta-bold-webfont weight 700, color #000000. 1px solid #d8d8d8 bottom border. Transition on scroll: opacity 1s for menu state changes. Zero border-radius on the header container."

> "Create a dark brand section: background #000000, text #ffffff. Headline at 35px bottegaveneta-regular-webfont weight 400, line-height 1.2. Subtitle at 14px weight 400, color #ffffff. CTA at 14px bottegaveneta-bold-webfont weight 700, color #ffffff, outline 1px solid #ffffff, background transparent, padding 15px 0, zero border-radius. No gradient, no glow, no shadow."

> "Create a newsletter signup form: input with underline-only border (bottom 1px solid #d8d8d8), zero border-radius, 14px bottegaveneta-regular-webfont weight 400, color #000000. Placeholder color #767676. On focus, border changes to 1px solid #000000. Label above the input at 12px weight 400, color #272727. Submit button: solid black #000000 background, white text, 14px bold, zero border-radius."

### Iteration Guide

1. Always use `bottegaveneta-regular-webfont` for body text and headings, `bottegaveneta-bold-webfont` ONLY for buttons and navigation — mixing bold into headings breaks the brand voice
2. Set `border-radius: 0` on every interactive element — if you see rounded corners, the design has drifted from Bottega Veneta. This is non-negotiable.
3. The entire color palette for UI is: `#000000`, `#272727`, `#474747`, `#767676`, `#bfbfbf`, `#d8d8d8`, `#f1f3f2`, `#ffffff`. Do not introduce any color outside this grayscale unless it is a functional marker (error red `#eb0000`, success green `#009539`).
4. Shadows are absent by design. If you catch yourself adding `box-shadow` to any element, remove it. The only acceptable depth treatment is the `rgba(0,0,0,0.2)` overlay behind modals.
5. Links are black `#000000`, weight 400 — never blue, never a different color. Hover state for links uses underline or slight background shift to `#eeeeee`, not color change.
6. Transition timing is `0.2s linear` for micro-interactions and up to `1s` for navigation state changes. Do not use `ease-in-out` or spring curves — BV's motion language is linear and precise.
7. Product photography fills its container edge-to-edge. Never add padding, margins, rounded corners, or shadows to product images. The image IS the design element.
8. When in doubt about spacing, go more generous. BV uses 80-160px between major sections. Whitespace is expensive and intentional — compress it and the design loses its luxury signal.

## 10. Protocolo Luxo — Strategic Analysis

> This section is consumed by art-director, designer-agent, typography-specialist, and visual-qa-agent.
> It contextualizes Bottega Veneta as the #1 luxury minimal digital reference for adaptation in REIS [IA] luxury client projects.

### Why Bottega Veneta is the Reference (2025-2026)

Bottega Veneta under Matthieu Blazy (Creative Director since Nov 2021) represents the apex of "stealth luxury" in digital. The brand deleted all social media accounts in January 2021 — a move that communicated: we do not need to participate in the attention economy. The website inherited this philosophy and became the single canonical digital touchpoint.

What makes it the #1 reference:

1. **Radical restraint over trend-chasing** — While Gucci, Balenciaga, and Prada experiment with maximalist digital (3D worlds, AR try-on, gamification), BV does less. The competitive advantage is subtraction.
2. **Custom typeface as sole identity carrier** — No logo on products, no monogram pattern, no signature color. The proprietary BV serif carries 100% of the brand voice digitally.
3. **Zero border-radius discipline** — Every interactive element is a sharp rectangle. This is the most consistent radius=0 implementation across any luxury site. It reads as architectural precision.
4. **Photography-first hierarchy** — The UI exists only to frame product photography. No element competes with the image. This is the opposite of content-marketing-driven sites.
5. **Flat by conviction, not by trend** — No shadows, no elevation, no glassmorphism. The interface pretends it is a printed page. This predates and outlasts flat-design trends because it is philosophical, not aesthetic.
6. **Motion austerity** — Pure CSS animations, no GSAP/Framer/Lottie. Motion serves navigation (drawers, modals, sticky header), never decoration. The carousel auto-advance is the most animated element.
7. **Monochrome UI, chromatic product** — The entire UI is grayscale. Only product photography carries color. This creates a gallery-wall effect where the interface disappears and the craft speaks.

### Comparative Positioning

| Brand | Typography | Color Accent | Motion | Shadow/Depth | Border Radius | Philosophy |
|-------|-----------|-------------|--------|-------------|---------------|-----------|
| **Bottega Veneta** | Custom serif, 400 weight | None (black only) | CSS-only, functional | Zero | 0px everywhere | Printed catalog |
| Hermes | Custom serif | Orange #FF6600 | Playful illustration | Minimal | Mixed | Editorial whimsy |
| Celine | Custom sans, all-caps | None | Minimal | Zero | 0px | Rock editorial |
| The Row | System serif (Georgia) | None | None | Zero | 0px | Ultra-quiet |
| Prada | Custom sans | None | WebGL/3D | Minimal | Small | Tech-forward |
| Apple | SF Pro | Blue #007AFF | ScrollTrigger, 3D | Layered | Rounded | Product theater |
| REIS [IA] | Inter | Blue #4A90FF | GSAP + ScrollTrigger | Ambient glow | Minimal | Premium tech |

### Adaptation Rules for REIS [IA] Luxury Projects

When a REIS [IA] client project calls for BV-level luxury minimalism:

1. **Typography budget = 1 typeface, 2 weights max.** Select a serif or geometric sans that carries personality alone. No mixing families.
2. **Color budget = black + white + 1 functional color (error).** If the client needs a brand color, it must be used at <5% of surface area.
3. **Border-radius: 0px.** Non-negotiable for luxury. Rounded corners signal consumer/SaaS, not craft/luxury.
4. **No box-shadow.** Depth comes from borders (1px solid #D8D8D8) and photography, not elevation.
5. **Photography quality is a hard dependency.** This design system FAILS with mediocre imagery. If client photography is not gallery-grade, do not use this reference.
6. **Whitespace minimum: 80px between sections.** Less than this and the layout reads as e-commerce, not luxury.
7. **No decorative motion.** Transitions for navigation and state changes only. No scroll-triggered reveals, no parallax, no entrance animations.
8. **font-display: optional.** Luxury confidence means the page loads clean even without the custom font. No FOUT tolerance.
9. **Button vocabulary: 2 variants max.** Filled (primary) and outlined (secondary). No ghost, pill, gradient, or icon buttons.
10. **Navigation must disappear.** The header should be minimal enough that you forget it is there until you need it.

### Extraction Metadata

- **Method**: Static HTML/CSS analysis via design-md skill (automated phases 1-5) + Opus LLM synthesis (phase 6) + research-based enrichment
- **Confidence**: HIGH — Site served full HTML/CSS to the scraper (Salesforce Commerce Cloud does not use SPA/client-side rendering)
- **CSS analyzed**: 4.97MB (style.css monolith)
- **Tokens detected**: 33 CSS vars, 5 @font-face declarations, 27 unique tokens
- **Motion**: 10 duration values, 9 easing curves, 26 keyframe animations
- **Quality score**: B (88/100) — A in color, typography, spacing, border, lint; F in tokenization (site uses hardcoded values, not CSS vars); C in accessibility
- **Platform**: Salesforce Commerce Cloud (Demandware), Cloudflare CDN, Google Analytics/GTM
- **Date**: 2026-05-18
